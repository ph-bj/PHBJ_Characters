1. Use `pre_commit_instructions` to obtain the exact required pre-commit tasks and execute them using `run_in_bash_session`.
2. Use the `submit` tool to finalize the changes and submit the pull request.
