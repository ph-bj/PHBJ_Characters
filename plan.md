1. Use `read_file` to read `src/data.ts` to verify the generated relationships have been correctly inserted (since they have just been inserted).
2. Use `run_in_bash_session` to run `npm run lint` and `npx tsc --noEmit`.
3. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
4. Use `submit` to push the changes adding missing character relationships to `src/data.ts`.
