/**
 * Copy this file into `data/` with any unique descriptive name
 * (e.g. `data/poetry-competitions.ts`). When copying, change the import below to:
 * `import type { QuestionDefinition } from "../types";`
 * Do not edit `index.ts` — questions are discovered automatically.
 */
import type { QuestionDefinition } from "./types";

export const question: QuestionDefinition = {
  categoryZh: "",
  categoryEn: "",
  questionEn: "",
  questionZh: "",
  answerEn: "",
  answerZh: "",
};
